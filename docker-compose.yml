version: "2.2"

services:
  data_db:
    image: postgres:14.7
    container_name: "db.hermes"
    restart: always
    command: -p 8801
    ports:
      - "8801:8801"
    environment:
      PGDATA: /var/lib/postgresql/data/pgdata
      POSTGRES_DB: hermes_db
      POSTGRES_USER: hermes_db_user
      POSTGRES_PASSWORD: product-defeat-follow-worshiper-swimwear-drown
    volumes:
      - .db:/var/lib/postgresql/data
  data_web:
    image: ghcr.io/skelmis/hermes:master
    container_name: "web_hermes"
    restart: always
    ports:
      - "8800:8800"
    depends_on:
      - data_db
    cpus: 1
    mem_limit: 1g
    mem_reservation: 256m
    environment:
      SERVING_DOMAIN: "hermes.skelmis.co.nz"
      POSTGRES_HOST: db.hermes
      POSTGRES_DB: hermes_db
      POSTGRES_USER: hermes_db_user
      POSTGRES_PASSWORD: product-defeat-follow-worshiper-swimwear-drown
      POSTGRES_PORT: 8801

#networks:
#  default:
#    name: spook
#    external: true